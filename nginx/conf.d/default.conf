server {
    listen 80;
    server_name localhost geller.co.nz www.geller.co.nz;
    
    # Static files
    location /static/ {
        alias /var/www/static/;
        expires 30d;
    }
    
    # Media files
    location /media/ {
        alias /var/www/media/;
        expires 7d;
    }
    
    # Django application
    location / {
        proxy_pass http://web:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# SSL Configuration - Commented out until SSL certs are available
#server {
#    listen 443 ssl;
#    server_name geller.co.nz www.geller.co.nz;
#
#    ssl_certificate /etc/nginx/ssl/fullchain.pem;
#    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#    
#    # SSL configuration
#    ssl_protocols TLSv1.2 TLSv1.3;
#    ssl_ciphers HIGH:!aNULL:!MD5;
#    ssl_prefer_server_ciphers on;
#    
#    # Static files
#    location /static/ {
#        alias /var/www/static/;
#        expires 30d;
#    }
#    
#    # Media files
#    location /media/ {
#        alias /var/www/media/;
#        expires 7d;
#    }
#    
#    # Django application
#    location / {
#        proxy_pass http://web:8000;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#    }
#}